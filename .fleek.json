{
 "build": {
   "image": "node:alpine",
   "command": "npm install pnpm && pnpm install && pnpm run build && echo $SOME_ENV",
   "publicDir": "build",
   "environment": {
     "SOME_ENV": "Build finished!"
   }
 }
}
